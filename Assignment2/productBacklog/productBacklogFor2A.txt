The shell allow the user to continiously input commands.  ~  3 hr
As an user, the command "exit" made me quit the program.  ~  1 hr
As an user, the command "mkdir" created two directories I specified.  ~  1 hr
As an user, the command "mkdir" didn't make any directories I specified because there was an error on the creation of the first directory.  ~  1 hr
As an user, the command "mkdir" only made the first directory I specified because there was an issue with the creation of the second directory.  ~  1 hr
As an user, the command "cd" changed the current directory to the directory I specified.  ~  1 hr
